server:
  port: 8080

spring:
  application:
    name: iqro
  datasource:
    url: jdbc:postgresql://localhost:5432/iqro
    username: postgres
    password: 91926499
    driver-class-name: org.postgresql.Driver

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect

logging:
  level:
    org:
      springframework:
        security: DEBUG